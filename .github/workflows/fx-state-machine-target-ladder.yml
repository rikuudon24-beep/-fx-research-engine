name: FX State Machine Target Ladder
on:
  workflow_dispatch:
  push:
    branches: [main]
jobs:
  research:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - run: python scripts/research_state_machine_target_ladder.py
      - uses: actions/upload-artifact@v4
        with:
          name: state-machine-target-ladder
          path: reports/state_machine_target_ladder_*.csv
