name: FX Pin Bear Focus Research

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "scripts/research_50pip_pin_bear_focus.py"
      - "scripts/research_50pip_direct_entry.py"
      - "data/market/**"
      - ".github/workflows/fx-pin-bear-focus.yml"

permissions:
  contents: write

concurrency:
  group: fx-pin-bear-focus
  cancel-in-progress: false

jobs:
  pin-focus:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check pandas numpy
      - name: Run pin-bear focus research
        run: python -u scripts/research_50pip_pin_bear_focus.py
      - name: Upload research reports
        uses: actions/upload-artifact@v4
        with:
          name: 50pip-pin-bear-focus
          path: |
            reports/50pip_pin_bear_focus.csv
            reports/50pip_pin_bear_extension.csv
      - name: Persist reports
        run: |
          git config user.name "fx-research-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/50pip_pin_bear_focus.csv reports/50pip_pin_bear_extension.csv
          git diff --cached --quiet || git commit -m "research: save pin-bear focus results"
          git fetch origin main
          git rebase origin/main
          git push
